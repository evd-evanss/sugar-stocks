<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name="com.sugarspoon.pocketstocks.PocketStocksApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PocketFinance"
        tools:targetApi="31">
        <activity
            android:name="com.sugarspoon.pocketstocks.ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.PocketFinance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.sugarspoon.pocketstocks.ui.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PocketFinance">
        </activity>
        <activity
            android:name="com.sugarspoon.pocketstocks.ui.details.DetailsActivity"
            android:exported="true"
            android:theme="@style/Theme.PocketFinance" />
    </application>

</manifest>